<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.sheeta1998.mapper.StudentMapper">
    <insert id="insert">
        insert into student (name,no,gender,phone,degree,clazz_id,id_card,is_college,address,graduation_date)
        values (#{name},#{no},#{gender},#{phone},#{degree},#{clazzId},#{idCard},#{isCollege},#{address},#{graduationDate})
    </insert>

    <update id="update">
        update student s
        set
            s.name = #{name},
            s.no = #{no},
            s.phone = #{phone},
            s.gender = #{gender},
            s.degree = #{degree},
            s.id_card = #{idCard},
            s.is_college = #{isCollege},
            s.address = #{address},
            s.graduation_date = #{graduationDate},
            s.violation_count = #{violationCount},
            s.violation_score = #{violationScore},
            s.clazz_id = #{clazzId}
        where s.id=#{id}
    </update>

    <delete id="deleteBatch">
        delete from student s where s.id in
        <foreach collection="ids" item="id" separator="," open="(" close=")">
            #{id}
        </foreach>

    </delete>

    <select id="count" resultType="java.lang.Long">
        select count(*) from student
    </select>

    <select id="list" resultType="com.sheeta1998.pojo.Student">
        select s.* from student s
        <where>
        <if test="name!=null and name !=''">
            s.name like Concat('%',#{name},'%')
        </if>
        <if test="degree!=null">
            and s.degree = #{degree}
        </if>
        <if test="clazzId!=null">
            and s.clazz_id=#{clazzId}
        </if>
        </where>
    </select>

    <select id="findById" resultType="com.sheeta1998.pojo.Student">
        select * from student where  id=#{id}
    </select>

    <select id="countStudentsEveryClazz" resultType="java.util.Map">
        select  count(*) data_list, c.name job_list
        from student s
            left join clazz c on c.id = s.clazz_id
        group by c.name
        order by data_list asc
    </select>

    <select id="getDegreeData" resultType="java.util.Map">
        select   (case degree
                      when 1 then '初中'
                      when 2 then '高中'
                      when 3 then '大专'
                      when 4 then '本科'
                      when 5 then '硕士'
                      when 6 then '博士'
            end)  name,     count(student.degree) as value from student group by degree
                                                                        order by degree

    </select>
</mapper>